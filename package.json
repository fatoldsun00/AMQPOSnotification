{
  "name": "amqpnotifier",
  "version": "1",
  "description": "Natif notification throught node-notifier. Fire on AMQP topic msg receive",
  "main": "main.js",
  "dependencies": {
    "amqplib": "^0.6.0",
    "electron-updater": "^4.3.5",
    "node-notifier": "^7.0.0"
  },
  "devDependencies": {
    "electron": "^8.2.1",
    "electron-packager": "^14.2.1",
    "electron-builder": "^22.4.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "deploy": "electron-builder build --mac --linux --win --publish always"
  },
  "author": "FatOldSun00",
  "license": "ISC"
}
